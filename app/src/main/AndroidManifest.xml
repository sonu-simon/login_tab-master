<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.codingdemos.tablayout">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".scheme2"></activity>
        <activity android:name=".farm_main" />
        <activity
            android:name=".farm_log"
            android:label="@string/title_activity_farm_log"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".ui.login.login_farm"
            android:label="@string/title_activity_login_farm" />
        <activity android:name=".registerScreen" />
        <activity android:name=".scheme1" />
        <activity android:name=".UserSelector">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".yojanas" />
        <activity android:name=".webView" />
        <activity android:name=".login" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>